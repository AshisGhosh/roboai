cd /isaac-sim/humble_ws 
if [ "$#" -eq 1 ]; then
    PKG_ARG="$1"
    echo "Building specific package: $PKG_ARG"
    colcon build --packages-select "$PKG_ARG"
else
    echo "Building all packages"
    colcon build
fi
cd -